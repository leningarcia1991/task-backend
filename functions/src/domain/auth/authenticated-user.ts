/**
 * Interface representing an authenticated user.
 * Contains essential user information extracted from authentication tokens.
 */
export interface AuthenticatedUser {
  id: string;
  email: string;
}
